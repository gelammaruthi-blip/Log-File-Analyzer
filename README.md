# Log-File-Analyzer
Log File Analyzer is a Python-based cybersecurity project that analyzes log files to identify failed login attempts and track the IP addresses associated with them. The project reads log entries, detects failed login events, extracts IP addresses, and counts the number of failed attempts from each IP address.
